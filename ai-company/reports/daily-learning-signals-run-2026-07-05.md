# 日次学習シグナル保存実行レポート - 2026-07-05

## 結論
`npm.cmd run learn:signals` を実行しましたが、新規シグナルは0件でした。

実行後の保存済みシグナルは2,270件、保存済み答え合わせは5,864件です。

2026-07-05分は、1mが91件、5mが91件、1hが91件のままです。15m、4h、1dは0件です。

`npm.cmd test` は20件成功、0件失敗です。

外部決済、個人情報、実売買、Xへの直接投稿は0回です。

## 理由
通常実行の `npm.cmd run learn:signals` は完了しました。実行時間は378.9秒です。

サマリーは更新されましたが、`newSignals` は0件でした。

代表銘柄として XAUUSD、SPY、USDJPY、7203JP を確認しました。15m、4h、1dの12組すべてで価格データが0本でした。

各組の理由は「必要な価格データが不足しています。必要90本、取得0本です。」でした。

権限付きで `npm.cmd run learn:signals` を再実行しましたが、664,066ミリ秒で時間切れになりました。残ったNodeプロセスは停止済みです。

重複は実行前後で増えていません。`日付|提供元|時間足|銘柄` の重複行は91行のままです。

## 現在の保存状況
| 項目 | 件数 |
| --- | ---: |
| 保存済みシグナル | 2,270 |
| 保存済み答え合わせ | 5,864 |
| 一意のシグナルキー | 2,179 |
| 重複行 | 91 |
| 今回の新規シグナル | 0 |
| 今回の新規答え合わせ | 0 |

## 2026-07-05分の時間足
| 時間足 | 件数 |
| --- | ---: |
| 1m | 91 |
| 5m | 91 |
| 15m | 0 |
| 1h | 91 |
| 4h | 0 |
| 1d | 0 |

## 次のアクション
1. 推奨案: 15m、4h、1dを時間足ごとに分けて、短い上限時間で実行する。失敗した時間足だけを再実行できるようにする。
2. 選択肢2: 外部データ提供元の状態が戻るまで待ち、次回の自動実行で再確認する。
3. 選択肢3: 今日分だけ `demo` 提供元で埋める。ただし無料外部データの結果とは別物になります。

推奨は1です。理由は、今回の通常実行では完了しても0件、権限付き実行では時間切れになったため、6時間足まとめでは原因の切り分けが遅くなるからです。
