# Market AI Daily Learning Report - 2026-07-05

## 結論
2026-07-05の日次学習は部分成功です。

指定コマンド `node scripts/daily-learn.js --provider=free-composite --intervals=1m,5m,15m,1h,4h,1d` は660,310ミリ秒で時間切れになりました。

回復として5mだけを分割実行し、5mの新規シグナル91件は `data/learning/signals.jsonl` に保存できました。答え合わせの新規保存は0件で、`learning-summary.json` と `learning-summary.md` は更新されていません。

`npm.cmd test` は19件成功、0件失敗です。

外部決済、個人情報、実売買、Xへの直接投稿は0回です。

## 理由
実行前のシグナルは2,179件、実行後は2,270件です。今回増えたシグナルは91件です。

実行後のシグナル内訳は、累計で1mが455件、5mが364件、15mが273件、1hが421件、4hが364件、1dが226件、時間足不明が167件です。

2026-07-05分のシグナルは273件です。内訳は1mが91件、5mが91件、1hが91件です。15m、4h、1dの2026-07-05分シグナルはまだ保存できていません。

答え合わせは実行前後とも5,864件で、今回の増加は0件です。読み取り失敗はシグナル0件、答え合わせ0件です。

重複は `日付|提供元|時間足|銘柄` の組み合わせで91行あります。今回の5m追加後も重複増加は確認していません。

`learning-summary.json` の最終更新は2026-07-05 01:04:43 JSTのままで、内容は1mの実行結果です。そのため、現在のサマリーは5mの追加91件をまだ反映していません。

5mの分割実行では、シグナル91件を追記したあと、答え合わせ処理中に長時間止まりました。私が起動した残プロセスは停止済みです。

最終テスト結果は19件成功、0件失敗、実行時間1,970.8327ミリ秒です。

## 次のアクション
1. 推奨案: `daily-learn.js` を「シグナル保存」と「答え合わせ保存」に分け、保存直後にサマリーを更新できる形へ直す。その後、未保存の15m、4h、1dと、5mの未完了答え合わせを実行する。
2. 選択肢2: 15m、4h、1dを1つずつ長い時間で実行する。追加改修は不要ですが、今回と同じように答え合わせで止まる可能性があります。
3. 選択肢3: 今日だけ `demo` 提供元で全6足を埋める。処理は軽いですが、無料外部データの学習結果とは別物になります。

推奨は1です。理由は、今の処理はシグナル保存後に全過去シグナルの答え合わせをまとめて進めるため、1つの時間足でも完了前に長時間止まりやすいからです。
