# 相場分析AI 日次学習レポート 2026-07-21

## 結論

日次学習は最終的に完了しました。通常権限の1回目は外部価格データが0本で新規保存なしでしたが、外部データ取得を許可した再実行と `1d` 単独再実行で、2026-07-21分の全6足を保存できました。

- 対象: 91銘柄 × 6足 = 546件
- 2026-07-21の保存済みシグナル: 546/546件
- 今回増えたシグナル: 546件
- 今回増えた答え合わせ: 1,577件
- 最終累計シグナル: 3,887行
- 最終累計答え合わせ: 11,499行
- `learning-summary.json` の未評価: 162件
- 重複を除いた未保存の答え合わせID: 798件
- `npm.cmd test`: 22件成功、0件失敗
- 外部決済、個人情報処理、実売買、Xへの直接投稿: 0件

## 理由

最初に指定コマンドを通常権限で実行しました。

```powershell
node scripts/daily-learn.js --provider=free-composite "--intervals=1m,5m,15m,1h,4h,1d"
```

この実行は 725.9秒で終了し、コマンド上は6足すべて成功でした。ただし、代表チェックでは `SPY 1m`、`SPY 1d`、`USDJPY 1d` がいずれも価格データ0本でした。取得元別チェックでも Yahoo は `HTTP 0`、Stooq は接続切断でした。そのため通常権限では新規シグナル0件、新規答え合わせ0件でした。

外部データ取得を許可して同じ全6足を再実行したところ、1,204.0秒でタイムアウトしました。ただし途中保存により `1m`、`5m`、`15m`、`1h`、`4h` の5足で 455件の新規シグナルと 1,265件の新規答え合わせが保存されました。

残った `1d` は単独で再実行しました。

```powershell
node scripts/daily-learn.js --provider=free-composite "--intervals=1d"
```

この実行は 778.2秒で正常終了し、`1d` の新規シグナル91件と新規答え合わせ312件が保存されました。

最終確認では、2026-07-21のシグナル保存数は次の通りです。

| 時間足 | 保存件数 |
| --- | ---: |
| 1m | 91 |
| 5m | 91 |
| 15m | 91 |
| 1h | 91 |
| 4h | 91 |
| 1d | 91 |

答え合わせの最終状態は次の通りです。

- 累計行数: 11,499行
- 重複を除いた答え合わせID: 10,590件
- 既存重複ID: 909件
- 2026-07-21に保存された答え合わせ: 1,577件
- 2026-07-21の内訳: `1m` 207件、`5m` 205件、`15m` 202件、`1h` 199件、`4h` 452件、`1d` 291件、`unknown` 21件

未評価の見方には2種類あります。`learning-summary.json` の未評価は 162件です。一方、重複を除いて `シグナルID × 4本後/8本後/16本後` で数える未保存IDは 798件です。内訳は `1d` 395件、`4h` 85件、`1h` 81件、`15m` 71件、`5m` 68件、`1m` 66件、`unknown` 32件です。

## 次のアクション

1. 次回も外部データ取得が通常権限で0本になる場合は、最初から外部データ取得を許可して実行する。
2. 未評価798件は、今後の価格データが増えた後に `npm.cmd run learn:outcomes` で答え合わせだけ追加する。
3. 全6足を1回で回すと20分を超える可能性があるため、今回と同じく途中で止まった場合は残った時間足だけ単独実行する。

推奨案は2です。今日のシグナル546件は保存済みなので、次回は新しい価格データが増えてから答え合わせだけを先に増やすのが最短です。

## 実行ログ要約

- 通常権限の全6足: 725.9秒、終了コード0、新規保存0件
- 通常権限の代表診断: 3件中3件が価格データ0本
- 取得元別診断: Yahoo `HTTP 0`、Stooq 接続切断
- 外部データ取得許可の全6足: 1,204.0秒でタイムアウト、5足まで途中保存
- 残プロセス: 停止済み、その後の確認で `node` 残プロセス0件
- 外部データ取得許可の `1d` 単独: 778.2秒、終了コード0
- 最終テスト: 22件成功、0件失敗、1,946.4ms
