# 相場分析AI 日次学習レポート 2026-07-14

## 結論
- 日次学習は完了しました。`free-composite` で `1m`, `5m`, `15m`, `1h`, `4h`, `1d` の6区間すべて成功しました。
- 実行時間は約736.6秒でした。区間別では `1m` 88.3秒、`5m` 48.4秒、`15m` 49.5秒、`1h` 52.3秒、`4h` 85.0秒、`1d` 412.7秒でした。
- 新規シグナルは0件、新規答え合わせは0件でした。累計はシグナル3341件、答え合わせ9922件、未評価101件です。
- `npm.cmd test` は22件中22件成功しました。
- 外部決済、個人情報、実売買、Xへの直接投稿は行っていません。

## 理由
- 実行コマンドは `node scripts/daily-learn.js --provider=free-composite "--intervals=1m,5m,15m,1h,4h,1d"` です。
- 対象は91銘柄と6区間なので、確認対象は合計546組です。
- 実行出力では6区間すべてが成功し、失敗区間は0件でした。
- 保存ファイルを確認した結果、`data/learning/signals.jsonl` は3341行、重複を除いたキーは3250件、既存の重複キーは91件でした。
- シグナルの最新保存日は2026-07-09で、最新日の内訳は `1m=91`, `5m=91`, `15m=91`, `1h=91`, `4h=91`, `1d=91` です。合計は546/546で、最新保存日の対象範囲は全件そろっています。
- `data/learning/outcomes.jsonl` は9922行、重複を除いたIDは9013件、既存の重複IDは909件でした。
- 答え合わせの最新日は2026-07-13で、最新日の内訳は `1m=22`, `5m=64`, `15m=63`, `1h=81`, `4h=79`, `1d=86` でした。
- `data/learning/learning-summary.json` は分割実行の最後の `1d` 子実行で上書きされるため、ファイル上の `intervals` は `["1d"]` になっています。全6区間の成功は、今回の実行出力と保存ファイルの件数で確認しました。
- 今回の追加保存が0件だった理由は、保存済みの最新有効日がすでに91銘柄と6区間でそろっており、今回の実行では追加で保存できる新しいシグナルと答え合わせが発生しなかったためです。

## 次のアクション
1. 明日も同じコマンドで日次学習を実行します。
2. 次回も新規シグナル0件が続く場合は、代表銘柄で `free-yahoo` と `free-stooq` の取得状況を比べます。
3. 未評価101件は、今後の足が増えたタイミングで答え合わせに回るかを確認します。
